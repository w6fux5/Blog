// import { UnauthenticatedError } from '../errors/index.js';

// const blockUser = (user) => {
//   if (user?.isBlocked) {
//     throw new UnauthenticatedError(`Access Denied ${user?.firstName} is blocked`);
//   }
// };

// export default blockUser;
